# 貢獻策略調查報告 (2026-02-07)

## 一、核心團隊成員與分工

| 成員 | 近 100 commits | 主要負責區域 |
|------|---------------|-------------|
| **Lancy** | 25 | CLI 功能開發、測試、storage、agent sharing |
| **Linghan Hu** | 19 | Slack 整合、Platform UI、Sentry |
| **Ethan Zhang** | 14 | DevOps/CI、測試基礎設施、eslint 規則 |
| **seven332** | 12 | Runner（Rust）、Firecracker、vsock |
| **Ming** | 8 | Platform UI、docs、blog |
| **fagemx（我們）** | 4 | web perf/test、cli test |

**關鍵觀察**：
- Lancy 密集在 CLI 加功能和測試（test(cli) 佔了大量 commits）
- Ethan 在建立測試基礎設施（msw wrapper、eslint 禁止直接 DB 存取規則）
- seven332 獨佔 runner 領域，全是 Rust 重寫
- 我們的 4 個 PR 涵蓋 web perf + test + cli test，方向正確

---

## 二、TODO 搜獵結果

### 可行動的 TODO（3 個）

| # | 檔案 | TODO 內容 | 可行性 |
|---|------|----------|--------|
| 1 | `packages/core/src/contracts/index.ts:11` | 升級 @ts-rest/core 到穩定版 3.53.0 | ⚠️ 需等上游發布穩定版 |
| 2 | `apps/runner/src/lib/config.ts:67` | 允許 port 0 自動找可用 port | ⚠️ 已有 issue #2197，已指派 seven332 |
| 3 | `apps/cli/src/lib/api/api-client.ts:107` | 用 @vm0/core 推斷型別取代手動定義 | ⚠️ 依賴 core 套件演進 |

### 複雜度標記 TODO（15 個）

全是 `// eslint-disable-next-line complexity -- TODO: refactor complex function`，分佈在：
- `web/app/v1/runs/` (3 個)
- `web/src/lib/` (schedule-service, storage-resolver, proxy-service, expand-environment)
- `cli/src/lib/` (yaml-validator, codex-event-renderer, codex-event-parser)
- `packages/core/src/sandbox/` (run-agent, direct-upload)
- `runner/src/lib/executor/`
- `platform/src/signals/fetch.ts`

**結論**：TODO 金礦基本被挖完。剩餘的要嘛等外部依賴、要嘛已被認領、要嘛是大型重構。

---

## 三、GitHub Issues 分析

### 未指派且可能可做的

| # | 標題 | 標籤 | 評估 |
|---|------|------|------|
| **#2510** | bug: Blog sharing links return incorrect URL in Safari | bug, ui, priority | ⭐ **最有機會** — UI bug，priority 標籤，無人認領 |
| **#2464** | failed to execute command "vm0 auth login" | network | 可能是用戶環境問題，需要更多資訊 |
| **#1327** | feat: add daily usage aggregation table and cron job | pending | 被標 pending，需先討論 |
| **#1283** | feat(runner): consider two-tier metrics architecture | later | 被標 later，暫緩 |

### 已指派（不碰）

| # | 標題 | 指派者 | 區域 |
|---|------|--------|------|
| #2505 | skeleton loading for logs page | Linghan Hu | platform |
| #2498 | duplicate text display bug | Linghan Hu + Ming | platform |
| #2493 | rename vm-common crates | seven332 | runner |
| #2486 | table header mislabel bug | Ethan + Ming | platform |
| #2485 | scheduled runs fail silently | Ethan + Ming | web |
| #2477 | rewrite runner in rust | seven332 | runner |
| #2471 | agent timeout in Slack | Linghan Hu | slack |
| #2467 | onboarding multi-provider | Linghan Hu | platform |
| #2462 | GitHub compose API for Slack | Lancy | web/slack |
| #2442 | GitHub OAuth connector | Lancy | web |
| #2421 | block-level COW storage | seven332 | runner |
| #2407 | OAuth connector | Ethan + Lancy | web |
| #2382 | job scheduling redesign | seven332 | runner |
| #2199, #2197, #2090, #2023, #2017, #1167 | runner 系列 | seven332 | runner |

**結論**：大部分 issues 已被核心團隊認領。#2510 (Safari blog bug) 是唯一未指派的 priority bug。

---

## 四、缺少測試的 API Routes

### 總覽
- **有測試的 routes**: 48 個 (55.8%)
- **沒有測試的 routes**: 38 個 (44.2%)

### Easy 難度（適合我們做的）

| Route | 功能 | 備註 |
|-------|------|------|
| `api/auth/me` | 取得當前用戶資訊 | 簡單 Clerk API |
| `api/cli/auth/device` | 產生 device code | 跟我們剛做的 token 是同一個 flow |
| `api/model-providers/check/[type]` | 檢查 provider secret 是否存在 | 簡單查詢 |
| `api/model-providers/[type]/convert` | 已棄用 endpoint | 只回傳錯誤 |
| `api/model-providers/[type]` | 刪除 model provider | 中等 |
| `api/model-providers/[type]/set-default` | 設定預設 provider | 中等 |
| `api/webhooks/agent/heartbeat` | Agent 心跳 | 簡單狀態更新 |

### Medium 難度

| Route | 功能 |
|-------|------|
| `v1/agents` | 列出 agents（分頁） |
| `v1/agents/[id]` | 取得 agent 詳情 |
| `v1/artifacts` | 列出 artifacts（分頁） |
| `v1/artifacts/[id]` | 取得 artifact 詳情 |
| `v1/volumes` | 列出 volumes（分頁） |
| `v1/volumes/[id]` | 取得 volume 詳情 |
| `v1/runs` (list) | 列出 runs |
| `v1/runs/[id]/cancel` | 取消 run |
| `api/agent/composes/[id]` | 取得 compose 詳情 |
| `api/storages/list` | 列出 storages |
| `api/realtime/token` | 產生 Ably token |

### Hard 難度（暫時不碰）

涉及 S3 presigned URL、Axiom 查詢、proxy 轉發、incremental upload 等。

---

## 五、推薦貢獻策略（優先順序）

### 策略 A：測試補完系列 ⭐⭐⭐ (最推薦)

**理由**：
- 我們剛成功做了 `cli/auth/token` 測試 PR，模式已熟悉
- Ethan 正在建測試基礎設施（eslint 規則、msw wrapper），說明團隊重視測試
- 38 個 route 沒測試 = 大量機會
- 不會跟核心團隊的功能開發衝突

**下一個目標候選**：
1. `api/cli/auth/device` — 跟剛做的 token 是同一個 auth flow，可以複用 helper
2. `api/model-providers/[type]` (delete) — model providers 是最近活躍區域
3. `api/model-providers/[type]/set-default` — 同上
4. `v1/agents` + `v1/agents/[id]` — public API 基礎 endpoint

### 策略 B：Bug 修復 ⭐⭐

**目標**：#2510 (Safari blog sharing links)
- 未指派、有 priority 標籤
- UI 層面的修復，風險低
- 需要先調查問題所在

### 策略 C：跟隨 PR 趨勢 ⭐

**觀察到的趨勢**：
- Model providers 正在被大量重構（rename credential → secret, multi-auth, settings page）
- 測試基礎設施在升級（msw wrapper, eslint 規則, api-test-helpers）
- CLI 在加新功能（porcelain, interactive mode, variable command）

**可做的**：跟在這些改動後面補測試或修小問題。

---

## 六、我們的已 Merge/進行中 PR

| PR | 標題 | 狀態 |
|----|------|------|
| #2373 | feat(web): make database pool settings configurable via env vars | ✅ Merged |
| #2445 | fix(web): exclude stale pending runs from concurrency check | ✅ Merged |
| #2465 | test(cli): replace internal config mock with vi.stubEnv in scope tests | ✅ Merged |
| #2501 | perf(web): replace N+1 queries with JOIN in runs list endpoint | ✅ Merged |
| #2508 | test(web): add integration tests for cli auth token exchange endpoint | ⏳ Open |

---

## 七、下一步行動

1. **等 #2508 merge**，確認 CI 通過
2. **開始策略 A**：挑 `api/cli/auth/device` 做下一個測試 PR
3. **順便看** #2510 (Safari bug) 是否容易修
4. **持續觀察** PR 趨勢，找順風車機會
